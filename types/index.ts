export type CategoryCard = {
  title: string;
  description: string;
  link: string;
  image: boolean;
};

export type ProductCard = {
  title: string;
  price: string;
  image: boolean;
  link: string;
};
